<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--				TEST SCENARIO				-->
<!-- REGISTER a user to the SUT and then keep the binding active active -->
<!-- by sending subsequent re-REGISTERs, before the existing binding 	-->
<!-- expires. The loop runs forever until the test is stopped.		-->

<scenario name="REGISTER">

  <nop>
  <action>
    <assign assign_to="5" value="0" />
    <!-- Capture start time in seconds for 59-minute timeout -->
    <gettimeofday assign_to="start_sec,start_usec" />
    <!-- Dummy test to suppress "referenced 1 times" warning for start_usec -->
    <test assign_to="98" variable="start_usec" compare="greater_than" value="-1" />
    <!-- Target end time = start + 3540 seconds (59 minutes) -->
    <assign assign_to="end_time" value="0" />
    <add assign_to="end_time" variable="start_sec" />
    <add assign_to="end_time" value="3540" />
  </action>
  </nop>

  <send retrans="1500" start_rtd="register">
    <![CDATA[

	REGISTER sip:[field2] SIP/2.0
	Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
	From: [field0] <sip:[field1]@[field2]>;tag=[call_number]
	To: <sip:[field1]@[field2]>
	CSeq: [cseq] REGISTER
	Call-ID: [call_id]
	Contact: <sip:[field1]@[local_ip]:[local_port]>
	Expires: [expires]
	User-Agent: [field0 file="random_user_agents.csv"]
	Accept-Language: en
	Max-Forwards: 70
	Content-Length: 0

    ]]>
  </send>

  <!-- <recv response="100" optional="true" rtd="register" repeat_rtd="true">  </recv> -->

  <recv response="407" auth="true" rtd="register">  </recv>

  <label id="10">  </label>
  <send retrans="1500" >
    <![CDATA[
	
	REGISTER sip:[field2] SIP/2.0
	Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
	From: [field0] <sip:[field1]@[field2]>;tag=[call_number]
	To: <sip:[field1]@[field2]>
	CSeq: [cseq] REGISTER
	Call-ID: [call_id]
  Contact: <sip:[field1]@[local_ip]:[local_port]>
  Expires: [expires]
	User-Agent: [field0 file="random_user_agents.csv"]
	Accept-Language: en
	[field3]
	Max-Forwards: 70
	Content-Length: 0

    ]]>
  </send>

  <!-- <recv response="100" optional="true"> </recv> -->

  <recv response="407" auth="true" optional="true" next="10">  </recv>

  <recv response="200" >
  <action>
        <ereg regexp="[0-9]+" search_in="hdr" header="Expires:" assign_to="1" />
        <add assign_to="5" value="1"/>
        <subtract assign_to="1" value="15"/>
        <todouble assign_to="2" variable="1"/>
        <multiply assign_to="2" value="1000"/>
    </action>
  </recv>
  <nop>
    <action>
      <!-- assign 7 to true if first pass -->
      <test assign_to="7" variable="5" compare="greater_than" value="1" />
    </action>
  </nop>
  <pause milliseconds="100" test="7" next="after_subscribe" >  </pause> 

  <label id="send_subscribe0"/>
  <send retrans="500" start_rtd="subscribeMwi">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1001@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70
      Accept: application/simple-message-summary
      [field3]
      Event: message-summary
      Expires: 3600
      Content-Length: 0

    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="407" auth="true" optional="true" next="send_subscribe0">  </recv>
                             
  <recv  response="200" rtd="subscribeMwi">
  </recv>

  <recv  request="NOTIFY"></recv>

  <send  >
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  <label id="send_subscribe1"/>
  <send retrans="500" start_rtd="subscribePresence">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1001@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      [field3]
      Accept: application/pidf+xml
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="407" auth="true" optional="true" next="send_subscribe1">  </recv>
                             
  <recv  response="200" rtd="subscribePresence">
  </recv>

  <recv  request="NOTIFY"></recv>

  <send  >
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  <label id="send_subscribe2"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1002@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      Accept: application/pidf+xml
      [field3]
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe2">  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->
  <recv  response="200" >
  </recv>

  <recv  request="NOTIFY"></recv>

  <send  >
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  <label id="send_subscribe3"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1003@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      [field3]
      Accept: application/pidf+xml
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe3">  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->
  <recv  response="200" >
  </recv>

  <recv  request="NOTIFY"></recv>

  <send  >
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  <label id="send_subscribe4"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1004@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      Accept: application/pidf+xml
      [field3]
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe4">  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->
  <recv  response="200" >
  </recv>

  <recv  request="NOTIFY"></recv>

  <send  >
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  <label id="send_subscribe5"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1005@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      Accept: application/pidf+xml
      Allow-Events: presence
      [field3]
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe5">  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->
  <recv  response="200" >
  </recv>

  <recv  request="NOTIFY"></recv>

  <send  >
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Length: 0

    ]]>
  </send>

  
  <label id="after_subscribe"/>
  <nop>
    <action>
      <!-- Get current time and check if we're still within the 59-minute window -->
      <gettimeofday assign_to="current_sec,current_usec" />
      <!-- Dummy test to suppress "referenced 1 times" warning for current_usec -->
      <test assign_to="99" variable="current_usec" compare="greater_than" value="-1" />
      <test assign_to="6" variable="current_sec" compare="less_than" variable2="end_time" />
    </action>
  </nop>

  <!-- Sleep based on the Expires value received, then loop back for re-REGISTER -->
  <pause variable="2" test="6" next="10">  </pause>
  <!-- Scenario ends here after 1 hour -->

  <ResponseTimeRepartition value="2, 5, 10, 20, 30, 40, 50, 60, 70, 80, 100, 125, 150, 175, 200, 250, 300, 400, 500, 600, 700, 800, 900, 1000, 2000"/>
  <CallLengthRepartition value="10"/>
</scenario>
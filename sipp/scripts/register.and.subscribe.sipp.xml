<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--				TEST SCENARIO				-->
<!-- REGISTER a user to the SUT and then keep the binding active active -->
<!-- by sending subsequent re-REGISTERs, before the existing binding 	-->
<!-- expires. The loop runs forever until the test is stopped.		-->

<scenario name="REGISTER">

  <nop>
  <action>
    <assign assign_to="5" value="0" />
    <!-- Loop counter for 59-minute timeout: default 78 loops × 45s = 58.5 min -->
    <!-- Using numeric variables: 8=loop_count, 9=max_loops -->
    <!-- max_loops comes from the launcher via -key reg_loops (register.sh -->
    <!-- passes 78; manual_test.sh can pass a small value so the post-loop -->
    <!-- extension call fires quickly when debugging) -->
    <assign assign_to="8" value="0" />
    <assignstr assign_to="reg_loops_s" value="[reg_loops]" />
    <todouble assign_to="9" variable="reg_loops_s" />
  </action>
  </nop>

  <send retrans="1500" start_rtd="register">
    <![CDATA[

	REGISTER sip:[field2] SIP/2.0
	Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
	From: [field0] <sip:[field1]@[field2]>;tag=[call_number]
	To: <sip:[field1]@[field2]>
	CSeq: [cseq] REGISTER
	Call-ID: [call_id]
	Contact: <sip:[field1]@[local_ip]:[local_port]>
	Expires: [expires]
	User-Agent: [field4]
	Accept-Language: en
	Max-Forwards: 70
	Content-Length: 0

    ]]>
  </send>

  <!-- <recv response="100" optional="true" rtd="register" repeat_rtd="true">  </recv> -->

  <recv response="407" auth="true" rtd="register">  </recv>

  <label id="10">  </label>
  <send retrans="1500" >
    <![CDATA[
	
	REGISTER sip:[field2] SIP/2.0
	Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
	From: [field0] <sip:[field1]@[field2]>;tag=[call_number]
	To: <sip:[field1]@[field2]>
	CSeq: [cseq] REGISTER
	Call-ID: [call_id]
  Contact: <sip:[field1]@[local_ip]:[local_port]>
  Expires: [expires]
	User-Agent: [field4]
	Accept-Language: en
	[field3]
	Max-Forwards: 70
	Content-Length: 0

    ]]>
  </send>

  <!-- <recv response="100" optional="true"> </recv> -->

  <recv response="407" auth="true" optional="true" next="10">  </recv>

  <recv response="200" >
  <action>
        <ereg regexp="[0-9]+" search_in="hdr" header="Expires:" assign_to="1" />
        <add assign_to="5" value="1"/>
        <subtract assign_to="1" value="15"/>
        <todouble assign_to="2" variable="1"/>
        <multiply assign_to="2" value="1000"/>
    </action>
  </recv>
  <nop>
    <action>
      <!-- assign 7 to true if first pass -->
      <test assign_to="7" variable="5" compare="greater_than" value="1" />
    </action>
  </nop>
  <pause milliseconds="100" test="7" next="after_subscribe" >  </pause> 

  <label id="send_subscribe0"/>
  <send retrans="500" start_rtd="subscribeMwi">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1001@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70
      Accept: application/simple-message-summary
      [field3]
      Event: message-summary
      Expires: 3600
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe0">  </recv>

  <recv response="200" rtd="subscribeMwi">
  </recv>

  <label id="send_subscribe1"/>
  <send retrans="500" start_rtd="subscribePresence">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1001@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      [field3]
      Accept: application/pidf+xml
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe1">  </recv>

  <recv  response="200" rtd="subscribePresence">
  </recv>

  <label id="send_subscribe2"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1002@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      Accept: application/pidf+xml
      [field3]
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe2">  </recv>

  <recv  response="200" >
  </recv>

  <label id="send_subscribe3"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1003@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      [field3]
      Accept: application/pidf+xml
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe3">  </recv>

  <recv  response="200" >
  </recv>

  <label id="send_subscribe4"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1004@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      Accept: application/pidf+xml
      [field3]
      Allow-Events: presence
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe4">  </recv>

  <recv  response="200" >
  </recv>

  <label id="send_subscribe5"/>
  <send retrans="500">
    <![CDATA[

      SUBSCRIBE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[field1]@[field2]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:1005@[field2]>
      Call-ID: [call_id]
      CSeq: 1 SUBSCRIBE
      Contact: sip:[field1]@[local_ip]:[local_port]
      Max-Forwards: 70  
      Event: presence
      Expires: 3600
      Accept: application/pidf+xml
      Allow-Events: presence
      [field3]
      Content-Length: 0

    ]]>
  </send>

  <recv response="407" auth="true" optional="true" next="send_subscribe5">  </recv>

  <recv  response="200" >
  </recv>

  <label id="after_subscribe"/>
  <nop>
    <action>
      <!-- Increment loop counter and check if we should continue -->
      <add assign_to="8" value="1" />
      <test assign_to="6" variable="8" compare="less_than" variable2="9" />
    </action>
  </nop>

  <!-- Sleep 45s then loop back for re-REGISTER if under max_loops (78 × 45s = 58.5 min) -->
  <pause milliseconds="45000" test="6" next="10">  </pause>

  <!-- ====== Re-registration loop done: probabilistic extension-to-extension call ====== -->
  <!-- Launcher passes -key extcall_pct N (0-100, percent of devices that place a call;   -->
  <!-- 0 disables). register.sh computes N from EXT_CALL_CPS_PCT x PEAK_CPS x the         -->
  <!-- business-day CPS curve, so calls only happen during the active call window.        -->
  <!-- Callee [field5] (own extension - 1, ext 1000 wraps to 1001) is appended to the     -->
  <!-- device CSV by normalize_device_csv in csv-utils.sh.                                -->
  <nop>
    <action>
      <assignstr assign_to="extcall_pct_s" value="[extcall_pct]" />
      <todouble  assign_to="extcall_pct"   variable="extcall_pct_s" />
      <sample    assign_to="extcall_rand"  distribution="uniform" min="0" max="100" />
      <test      assign_to="extcall_skip"  variable="extcall_rand"
                 compare="greater_than_equal" variable2="extcall_pct" />
    </action>
  </nop>
  <nop test="extcall_skip" next="extcall_end"/>

  <!-- Variable wait before dialing -->
  <pause min="5000" max="60000"/>

  <send retrans="500" start_rtd="extcall">
    <![CDATA[

      INVITE sip:[field5]@[field2] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field1]@[field2]>;tag=[call_number]ec
      To: <sip:[field5]@[field2]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:[field1]@[local_ip]:[local_port]
      User-Agent: [field4]
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 101
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-11,16

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="407" auth="true" rtd="extcall"/>

  <send>
    <![CDATA[

      ACK sip:[field5]@[field2] SIP/2.0
      [last_Via:]
      [last_From:]
      [last_To:]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <send retrans="500">
    <![CDATA[

      INVITE sip:[field5]@[field2] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field1]@[field2]>;tag=[call_number]ec
      To: <sip:[field5]@[field2]>
      Call-ID: [call_id]
      CSeq: 2 INVITE
      Contact: sip:[field1]@[local_ip]:[local_port]
      [field3]
      User-Agent: [field4]
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 101
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-11,16

    ]]>
  </send>

  <!-- Any outcome ends the scenario gracefully: 200 -> talk + BYE; the listed  -->
  <!-- finals (unregistered/busy/failed callee) -> ACK + end; no final within   -->
  <!-- 45s -> end. Expected 4xx must be ACKed explicitly or the SUT retransmits -->
  <!-- and the retransmission trips -abortunexp.                                -->
  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="403" optional="true" next="extcall_reject"/>
  <recv response="404" optional="true" next="extcall_reject"/>
  <recv response="408" optional="true" next="extcall_reject"/>
  <recv response="480" optional="true" next="extcall_reject"/>
  <recv response="486" optional="true" next="extcall_reject"/>
  <recv response="487" optional="true" next="extcall_reject"/>
  <recv response="500" optional="true" next="extcall_reject"/>
  <recv response="503" optional="true" next="extcall_reject"/>
  <recv response="600" optional="true" next="extcall_reject"/>
  <recv response="603" optional="true" next="extcall_reject"/>
  <recv response="200" crlf="true" timeout="45000" ontimeout="extcall_end"/>

  <send>
    <![CDATA[

      ACK sip:[field5]@[field2] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field1]@[field2]>;tag=[call_number]ec
      To: <sip:[field5]@[field2]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Caller-side audio (non-blocking), then a short talk time -->
  <nop>
    <action>
      <exec play_pcap_audio="g711a-orig.pcap"/>
    </action>
  </nop>
  <pause min="8000" max="30000"/>

  <send retrans="500">
    <![CDATA[

      BYE sip:[field5]@[field2] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [field0] <sip:[field1]@[field2]>;tag=[call_number]ec
      To: <sip:[field5]@[field2]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 3 BYE
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>
  <recv response="200" timeout="10000" ontimeout="extcall_end" next="extcall_end"/>

  <label id="extcall_reject"/>
  <send>
    <![CDATA[

      ACK sip:[field5]@[field2] SIP/2.0
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      CSeq: 2 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <label id="extcall_end"/>
  <nop/>
  <!-- Scenario ends here -->

  <ResponseTimeRepartition value="2, 5, 10, 20, 30, 40, 50, 60, 70, 80, 100, 125, 150, 175, 200, 250, 300, 400, 500, 600, 700, 800, 900, 1000, 2000"/>
  <CallLengthRepartition value="10"/>
</scenario>
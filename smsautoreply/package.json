{
  "name": "smsautoreply",
  "version": "1.0.0",
  "description": "Bandwidth.com inbound SMS/MMS webhook receiver that auto-replies with enriched data plus the contents of the inbound post",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "send": "node scripts/send-test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
